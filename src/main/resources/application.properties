spring.datasource.driver-class-name=${spring_datasource_driver}
# com.mysql.cj.jdbc.Driver
spring.datasource.url=${spring_datasource_url}
# jdbc:mysql://localhost:3306/full-stack-ecommerce
spring.datasource.username=${spring_datasource_username}
spring.datasource.password=${spring_datasource_password}

spring.jpa.properties.hibernate.dialect=${spring_jpa_properties}
# org.hibernate.dialect.MySQL8Dialect

spring.data.rest.base-path=${spring_data_rest_basepath}
#  /api

allowed.origins=${allowed_origins}
# https://mini-project-frontend-opal.vercel.app
# https://localhost:4200

# only expose end point with @Repository 
spring.data.rest.detection-strategy=${spring_data_rest_detection}
# ANNOTATED

okta.oauth2.client-id=${okta_oauth2_client}
# 0oa9on7muphrAqL8i5d7
okta.oauth2.issuer=${okta_oauth2_issuer}
# https://dev-35230924.okta.com/oauth2/default

# HTTPS configuration
# Server web port
# server.port=9898

# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=${server_ssl_enabled}
# true

# Alias that identifies the key in the key store
server.ssl.key-alias=${server_ssl_key}
# ecommerce

# Keystore location
server.ssl.key-store=${server_ssl_key_store}
#classpath:ecommerce-keystore.p12

# Keystore password
server.ssl.key-store-password=${server_ssl_key_password}
#secret

# Keystore format
server.ssl.key-store-type=${server_ssl_key_type}
# PKCS12

# Payment Processing with Stripe
stripe.key.secret=${stripe_key_secret}
#sk_test_51LSE1VHwGzJXhGX1nWGZse8Vu2Plb6oHOb8f6JIsVJDUTEXFcc9bKbyW7jC4QMVpa6bOGME64qGEDV2oLiFMwXa900ydby7wxu

# Redis Server Configuration
spring.redis.host=${spring_redis_host}
# localhost
spring.redis.port=${spring_redis_port}
# 6379